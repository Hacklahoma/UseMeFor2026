/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./src/**/*.{js,ts,jsx,tsx}" // Tells Tailwind to scan these files for class names
    ],
    theme: {
        extend: {},
    },
    plugins: [
        
    ],
};  